---
import Layout from "@layout/Layout.astro";
import GridCategorias from "@components/grid/gridCategorias";
const { id } = Astro.params;

// Import the necessary components and data
---

<Layout title={"Servicios al Vecino"}>
  <GridCategorias
    id={id}
    client:only="react"
    info="infoServicioVecino"
    url={"items/servicio_vecino?filter[categoria_serviciosVecino][_eq]="}
  />
</Layout>
